Use the agentprovider skill to build a fresh AWX provider with terraform-provider-dynamic.
Assume there are no existing AWX cassettes, contracts, Terraform examples, or Terraform state to reuse.

Cover these AWX surfaces:
- Resources with full CRUD: organization, inventory, job template, and host.
- Data source: look up an existing AWX object by id and expose computed fields.
- Actions:
  - awx_job_launch: POST /api/v2/job_templates/${id}/launch/
  - aap_workflow_job_launch: POST /api/v2/workflow_job_templates/${id}/launch/

Use local AWX on localhost

Workflow:
1. use agentprovider skill
2. create the Terraform example that uses the resources, data source, and both actions.
3. terraform init, terraform apply -auto-approve
4. cat the terraform code
5. Summarize the provider implementation, including how the resources, data source, and actions were implemented. Time from start to finish
